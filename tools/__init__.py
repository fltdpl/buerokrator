"""CLI-Werkzeuge rund um die App — nicht Teil der Anwendung selbst.

Aufruf immer als Modul aus der Repo-Wurzel, damit `src` im Importpfad liegt:

    python -m tools.evaluate --limit 40
    python -m tools.tax_check 2025
"""
