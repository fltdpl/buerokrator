[pytest]
; für die NiceGUI-User-Fixture (async) in tests/test_frontend_smoke.py
asyncio_mode = auto
; NiceGUI-Testplugin: App-Einstieg. Ohne das nähme es das Root-main.py, das
; die App nur startet und die Seiten nicht beim Import registriert.
main_file = src/frontend/main.py
