# Manuelle Prüfscripte

Diese Scripte sind keine automatisierten pytest-Tests.

Sie können echte OCR-, LLM-, Datei- oder Datenbankzugriffe ausführen und werden
deshalb nicht automatisch mit `python -m pytest` gestartet.

Automatisierte Tests liegen direkt unter `tests/` und heißen `test_*.py`.
